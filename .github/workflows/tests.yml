name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  fast-tests:
    name: Fast tests (CPU, no model download)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # Cache pip wheels so dependency install is fast on repeat runs.
      # Key includes pyproject.toml hash so a dep change busts the cache.
      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: pip-${{ runner.os }}-${{ hashFiles('pyproject.toml') }}
          restore-keys: pip-${{ runner.os }}-

      # Cache HuggingFace model weights — GPT-2 is ~500 MB.
      # Key is OS-only (no hash) so it survives pyproject.toml changes.
      - name: Cache HuggingFace models
        uses: actions/cache@v4
        with:
          path: ~/.cache/huggingface
          key: hf-gpt2-${{ runner.os }}
          restore-keys: hf-gpt2-

      - name: Install package + dev deps
        run: |
          pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Show installed versions
        run: pip list | grep -E "torch|transformer.lens|glassbox|pytest|sae"

      # Run tests and always write full output to the job summary so failures
      # are visible without needing admin log access.
      - name: Run fast tests
        run: |
          set +e
          pytest tests/ -v --tb=long -m "not slow" 2>&1 | tee pytest_output.txt
          PYTEST_EXIT=$?
          echo "## Fast Test Results" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          tail -100 pytest_output.txt >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          exit $PYTEST_EXIT
        timeout-minutes: 20

  slow-tests:
    name: Slow tests (GPU optional, full model suite)
    # Only run when manually triggered via workflow_dispatch, not on every push.
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: pip-${{ runner.os }}-${{ hashFiles('pyproject.toml') }}
          restore-keys: pip-${{ runner.os }}-

      - name: Cache HuggingFace models
        uses: actions/cache@v4
        with:
          path: ~/.cache/huggingface
          key: hf-gpt2-${{ runner.os }}
          restore-keys: hf-gpt2-

      - name: Install package + dev deps
        run: |
          pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Show installed versions
        run: pip list | grep -E "torch|transformer.lens|glassbox|pytest|sae"

      - name: Run full test suite (including slow)
        run: |
          set +e
          pytest tests/ -v --tb=long 2>&1 | tee pytest_output.txt
          PYTEST_EXIT=$?
          echo "## Full Test Results" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          tail -150 pytest_output.txt >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          exit $PYTEST_EXIT
        timeout-minutes: 25
